# merge-v146-p0-p1 design

Use cherry-pick/manual small patches rather than full upstream/main merge. Resolve conflicts in shared hot paths by preserving local behavior and adding upstream bugfix logic only.
