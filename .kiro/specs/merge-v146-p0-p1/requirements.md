# merge-v146-p0-p1 requirements

- Include confirmed P0, P1, and worth-updating upstream fixes/features only.
- Do not include Docker insecure defaults, batch image MVP, header override feature, EasyPay custom methods, or upstream pure-move refactors.
- Preserve local HK/JP custom behavior: gpt-5.5 routing, sticky/priority/failover, usage/billing incentives, safe deploy defaults.
